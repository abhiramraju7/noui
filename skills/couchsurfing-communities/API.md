# Couchsurfing Communities API

`list_community_links(query="", limit=20, profile_slug="couchsurfing")` returns public place-community links.

`get_place_details(url, profile_slug="couchsurfing")` returns public title, description, and a bounded text summary.
