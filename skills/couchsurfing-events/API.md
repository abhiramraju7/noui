# Couchsurfing Events API

`list_public_events(query="", limit=20, profile_slug="couchsurfing")` returns public event links.

`get_event_details(url, profile_slug="couchsurfing")` returns public title, description, and a bounded text summary.
