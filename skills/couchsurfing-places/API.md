# Couchsurfing Places API

`search_places(query, limit=10, profile_slug="couchsurfing")` returns matching public place links.

`list_popular_places(limit=20, profile_slug="couchsurfing")` returns public place links from the catalogue.
