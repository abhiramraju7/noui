[project]
name = "freshbooks-mark-payment"
version = "0.1.0"
description = "NoUI-generated skill for recording payments against FreshBooks invoices."
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "websockets>=12",
]

[tool.uv]
package = false
