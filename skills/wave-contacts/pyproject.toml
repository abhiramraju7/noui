[project]
name = "wave-contacts"
version = "0.1.0"
description = "NoUI skill for listing and creating Wave customers via Tabby-routed GraphQL."
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
]

[tool.uv]
package = false
