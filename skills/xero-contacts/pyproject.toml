[project]
name = "xero-contacts"
version = "0.1.0"
description = "NoUI-generated skill for listing and creating Xero contacts."
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "websockets>=12",
]

[tool.uv]
package = false
