[project]
name = "zoho-books-accounting"
version = "0.1.0"
description = "NoUI-generated skill for reading Zoho Books accounting reference data."
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "websockets>=12",
]

[tool.uv]
package = false
